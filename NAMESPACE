iHPOrtFrom(utils, getFromNamespace)
iHPOrt(methods)
iHPOrt(DBI)
iHPOrt(AnnotationDbi)
iHPOrt(AnnotationHub)
iHPOrtFrom(BiocFileCache, makeCachedActiveBinding)

### Only put what is statically exported here. All the AnnObj instances
### created at load time are dynamically exported (refer to R/zzz.R for
### the details).
export(
    HPO,
    HPO_dbconn,
    HPO_dbfile,
    HPO_dbschema,
    HPO_dbInfo,
    columns,
    keytypes,
    select,
    keys
)
